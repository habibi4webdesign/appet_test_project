
import React from 'react';

import {ProgressSpinner} from 'primereact/components/progressspinner/ProgressSpinner';

const spinner = () => (

    <ProgressSpinner style={{ position: 'fixed', left: '40%', top: '35%' }} />
 );

export default spinner;